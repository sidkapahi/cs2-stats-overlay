# assets

Static assets referenced by the project README.

Drop the header banner here as **`header.png`** (the CS2 Stats Overlay hero image).
The main [`README.md`](../README.md) references it as `assets/header.png`.
